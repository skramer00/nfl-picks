import { ImageResponse } from "next/og";
import { notFound } from "next/navigation";
import { getPublicShare } from "@/lib/sharedPredictionsServer";
export const size = { width: 1200, height: 630 }; export const contentType = "image/png";
export default async function Image({ params }: { params: Promise<{ token: string }> }) { const { token } = await params; const share = await getPublicShare(token, "power_rankings"); if (!share) notFound(); const teams = ((share.payload as { teams?: { abbreviation: string; name: string }[] }).teams ?? []).slice(0, 10); return new ImageResponse(<div style={{ width: "100%", height: "100%", display: "flex", flexDirection: "column", background: "#050505", color: "white", padding: "58px", fontFamily: "sans-serif" }}><div style={{ display: "flex", color: "#f59e0b", fontSize: 28 }}>🥨 PRETZEL QUEST</div><div style={{ display: "flex", fontSize: 52, fontWeight: 800, marginTop: 18 }}>{share.display_name}&apos;s Power Rankings</div><div style={{ display: "flex", flexWrap: "wrap", gap: 14, marginTop: 34 }}>{teams.map((team, index) => <div key={team.abbreviation} style={{ display: "flex", width: "48%", padding: "12px 18px", border: "1px solid #273244", borderRadius: 14, background: "#07101f", fontSize: 25 }}><span style={{ color: "#6b7280", width: 42 }}>{index + 1}</span><strong>{team.abbreviation}</strong><span style={{ marginLeft: 16, color: "#d1d5db" }}>{team.name}</span></div>)}</div></div>, size); }
